{
  "change": "add-superdesktop-shell-installer",
  "captured_at": "2026-08-17",
  "local_verification": {
    "mutation_performed": false,
    "commands": [
      "cargo fmt --all -- --check",
      "cargo check --workspace --offline",
      "cargo clippy --workspace --all-targets --offline -- -D warnings",
      "cargo test --workspace --offline",
      "openspec validate add-superdesktop-shell-installer --strict",
      "validate_tasks.py openspec/changes/add-superdesktop-shell-installer/tasks.md",
      "shell-installer enable (absolute app/guardian paths, dry-run only)",
      "scripts/package-superdesktop.ps1 -SuperExplorerPath D:/SuperExplorer/target/release/SuperExplorer.exe -OutputDirectory build/SuperDesktop-package-smoke"
    ],
    "covered": [
      "dry-run without complete authority",
      "compare-before-write drift rejection",
      "rollback-record failure before mutation",
      "readback failure with exact prior-value rollback",
      "rollback failure preserving recovery metadata",
      "exact absent-value restore",
      "machine-readable CLI error and audit output",
      "binary content drift rejection before mutation",
      "SuperDesktop and SuperDesktopGuardian VersionInfo identity admission",
      "restore remains reachable when product binaries are missing",
      "repair reuses the immutable recovery record",
      "repair rejects silent installed-binary path replacement",
      "repair failure restores the exact transaction pre-state",
      "rollback metadata path is absolute, included in the plan fingerprint, and reported as an affected target",
      "guardian recovery admission executes a five-second fail-closed read-only handshake before planning and immediately before mutation",
      "guardian handshake verifies an interactive non-Safe-Mode session and resolves the trusted Explorer recovery target without mutation",
      "partial or mismatched mutation authority is rejected instead of being mislabeled as dry-run",
      "post-plan failures include operation, fingerprint, pre/post observations, affected targets, timestamp, and failed terminal audit",
      "production enable and mutation revalidation require adjacent shell-provider-host, notification-area-host, and SuperExplorer regular non-reparse binaries",
      "release packaging emits app, guardian, installer, both isolated hosts, SuperExplorer, and a SHA-256 manifest without overwriting an existing output directory"
    ]
  },
  "physical_gate": {
    "status": "external_pending",
    "required_host": "Windows 11 build 26200.8875 + ExplorerPatcher 26100.8457.70.3 exact reference profile",
    "collector": "scripts/capture-shell-installer-physical-evidence.ps1",
    "phases": ["DryRun", "Enable", "AfterReboot", "Rollback"]
  }
}
