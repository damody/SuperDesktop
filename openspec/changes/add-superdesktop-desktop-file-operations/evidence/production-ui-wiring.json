{
  "schema_version": 1,
  "change": "add-superdesktop-desktop-file-operations",
  "result": "production_path_wired",
  "captured_at": "2026-08-17",
  "implemented": [
    "production desktop surfaces enumerate the authoritative user and public Desktop namespaces",
    "hidden and system entries remain excluded by the published visibility policy",
    "single click selects while double click and Enter activate the selected real item",
    "folders launch SuperExplorer with a validated canonical initial directory",
    "files launch through the Windows association admission adapter",
    "F5 performs authoritative namespace refresh and restores selection only for surviving stable identities",
    "Delete routes through a typed correlated operation and the Recycle Bin policy before refresh",
    "F2 provides inline rename input and preserves the editor when validation or collision admission fails",
    "Shift+Delete requires a default-no native warning confirmation before selecting the explicit permanent-delete policy",
    "dragging a desktop item onto an explicit folder destination moves within one Desktop origin and copies across user/public origins",
    "external file drops copy into the user Desktop using explicit OS-provided paths",
    "all production transfer destinations use collision-rename and never silently overwrite",
    "child key handling stops propagation so real-item activation cannot also trigger the fixed entry",
    "copy and move requests execute on a background worker while the GPUI desktop remains responsive",
    "a production transfer card reports byte progress and exposes cancellation backed by the operation controller cancellation contract",
    "recursive directory copies preserve nested and empty directories while reporting aggregate byte progress",
    "directory transfer admission rejects symbolic links and reparse points and revalidates every entry before mutation",
    "cancelled or failed directory copies remove the entire newly-created destination while leaving the source intact",
    "failed or cancelled operations publish terminal results for every remaining request in the background batch",
    "terminal transfer results reconcile every desktop surface with the authoritative namespace",
    "desktop icons use explicit logical positions and background drops persist per-monitor positions atomically",
    "background context commands select and persist name, kind, size, or modified ordering and ascending or descending direction"
  ],
  "deliberately_not_claimed_yet": [],
  "commands": [
    "cargo test -p platform-win -p desktop-ui -p superdesktop-app --offline",
    "cargo clippy -p platform-win -p desktop-ui -p superdesktop-app --all-targets --offline -- -D warnings",
    "cargo build -p superdesktop-app --offline",
    "target/debug/superdesktop-app.exe --verification-capture-ms 750"
  ]
}
