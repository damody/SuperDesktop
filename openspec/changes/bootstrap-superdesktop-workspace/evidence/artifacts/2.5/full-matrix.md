# Wave 1 corrective replacement matrix

Passed checks: three full current replacement manifests (workspace, dependency,
and source boundary), Draft 2020-12 record and coverage validation, the 21-case
isolated production mutation matrix, evidence validator, architecture positive
check and three expected-failure architecture fixtures, locked/offline workspace
check and tests, and `cargo fmt --check`.

The aggregate contract is `aggregate-contract-inputs.sha256`; its own SHA-256 is
the handoff contract hash. The contract inputs and raw fixture outputs are
preserved alongside this report.
This L2 leaves final reviewer acceptance and the independent 2.4.5 re-review to
the primary integrator.
