# Validator implementation

`scripts/validate-evidence.ps1` verifies append-only identities, artifact SHA-256 values, coverage, task checkboxes, mandatory status, replacement graph and adjustments lineage.
