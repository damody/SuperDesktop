{
  "schema_version": 1,
  "change": "add-superdesktop-shell-takeover-recovery",
  "result": "local_passed_reference_profile_finalization_pending",
  "captured_at": "2026-08-17",
  "covered": [
    "production shell lifecycle arms the adjacent restricted guardian before opening shell surfaces",
    "preview lifecycle does not spawn a guardian",
    "guardian spawn or acceptance failure rejects shell startup fail closed",
    "guardian waits for the full parent process lifetime instead of timing out after ten seconds",
    "guardian acknowledgement is bound to the sealed pipe nonce and cannot be satisfied by the fixed legacy marker",
    "parent closes all controller-side process, thread, and parent handles after authenticated acceptance",
    "guardian terminal records use an absolute SuperDesktop-owned LocalAppData path",
    "production success terminal is atomically created only after Explorer recovery succeeds",
    "reference-profile completion evidence force-crashes the real admitted production Shell process rather than an evidence-only parent fixture"
  ],
  "commands": [
    "cargo test -p platform-win -p superdesktop-guardian -p superdesktop-app --offline",
    "cargo clippy -p platform-win -p superdesktop-guardian -p superdesktop-app --all-targets --offline -- -D warnings"
  ],
  "external_gate": {
    "status": "external_pending",
    "required": "exact Windows 11 ExplorerPatcher candidate-bound ten-run forced-crash and recovery measurement"
  }
}
