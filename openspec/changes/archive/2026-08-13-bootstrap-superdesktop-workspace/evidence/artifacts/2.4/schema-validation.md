# Schema-complete evidence records

Corrective records use schema version 2.0.0 and require `procedure`, `expected`, and `actual`; the validator evaluates the JSON Schema rules rather than only checking hand-written field presence.
