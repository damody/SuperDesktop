# Immutable adjustment lineage

Each adjustment names stale and replacement `task_id#subcheck` identities. The validator requires exact backlinks, passed coverage-identical replacements, complete affected sets, and explicit C approval.
